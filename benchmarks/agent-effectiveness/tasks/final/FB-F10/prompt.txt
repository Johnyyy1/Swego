Workflow and workflow-run pagination cursors must accept only the canonical unpadded base64url encoding produced by the server. Reject tokens containing standard base64 characters, padding, whitespace, ignored trailing characters, or an excessively large encoded payload before JSON parsing. Valid server-generated cursors must keep round-tripping and invalid cursors must continue through the existing client-error type rather than becoming server errors.
