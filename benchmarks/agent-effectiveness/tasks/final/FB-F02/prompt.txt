The browser JavaScript SDK must accept an application URL with one or more trailing slashes and behave exactly as it does for the same URL without a trailing slash. Every API and survey-bundle URL constructed during setup, workspace loading, user updates, and survey preloading/loading must contain exactly one slash at the origin/path boundary. Preserve the existing public setup API and add regression coverage.
