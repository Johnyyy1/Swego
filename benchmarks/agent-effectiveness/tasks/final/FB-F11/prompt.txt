AI provider error classification must parse `Retry-After` according to its two valid forms: a non-negative integer number of seconds or an HTTP date. Header lookup must be case-insensitive. Negative, signed, decimal, partially numeric, or otherwise malformed delay values must be ignored rather than producing a delay. HTTP-date delays must remain clamped to zero when the date is in the past. Preserve the existing quota and retryability classification.
