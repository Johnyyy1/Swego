API key labels must be unique within an organization so that users cannot create two indistinguishable keys in the same tenant. The same label must still be allowed in different organizations. Enforce the invariant in the database schema with a forward migration, and preserve the existing database-error handling for create and rename collisions. Include tests or schema validation that demonstrates the tenant-scoped uniqueness.
