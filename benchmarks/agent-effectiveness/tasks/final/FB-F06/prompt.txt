Workflow list endpoints must reject ambiguous duplicate values for every single-valued query parameter instead of silently using the first value. This includes workspace, cursor, limit, sort, text, workflow/response IDs, and dry-run filters. Repeated or comma-separated status filters must continue to be supported. Invalid duplication should follow the existing invalid-input path and produce a client error before any database work.
