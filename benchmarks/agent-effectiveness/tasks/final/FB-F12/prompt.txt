Survey file uploads must accept both a complete base64 data URL and a raw base64 payload. Decode the payload before requesting upload credentials so empty or malformed data fails without making any network request. Successful uploads must send the same bytes and MIME type in either representation, and existing storage-configuration, file-size, and upload-service error behavior must remain intact.
